version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Environment setup
        - echo "🚀 Starting Amplify build process..."
        - echo "Branch: $AWS_BRANCH"
        - echo "Commit: $AWS_COMMIT_ID"
        
        # Use Node.js 22 LTS (matches CI and package.json engines)
        - nvm install 22
        - nvm use 22
        
        # Install yarn globally with version pinning
        - npm install -g yarn@1.22.22
        
        # Clear any existing cache issues
        - yarn cache clean
        
        # Install dependencies with optimizations
        - yarn install --frozen-lockfile --prefer-offline --network-timeout 100000
        
        # Verify installation
        - yarn --version
        - node --version
        
        # Pre-build validation
        - |
          if [ ! -f "package.json" ]; then
            echo "❌ package.json not found"
            exit 1
          fi
        
        - |
          if [ ! -f "yarn.lock" ]; then
            echo "❌ yarn.lock not found"
            exit 1
          fi
        
        # Check for critical dependencies
        - |
          if ! yarn list --pattern "vue" > /dev/null 2>&1; then
            echo "❌ Vue.js not found in dependencies"
            exit 1
          fi
        
    build:
      commands:
        # Conditional build based on branch
        - |
          if [ "$AWS_BRANCH" = "main" ]; then
            echo "🏗️ Production build for main branch"
            BUILD_MODE="production"
          elif [ "$AWS_BRANCH" = "develop" ]; then
            echo "🧪 Development build for develop branch"
            BUILD_MODE="development"
          else
            echo "🔧 Feature build for branch: $AWS_BRANCH"
            BUILD_MODE="development"
          fi
        
        # Run linting first (fail fast on code quality issues)
        - echo "🔍 Running linting checks..."
        - yarn lint
        
        # Run type checking (catch TypeScript errors early)
        - echo "🔧 Running type checking..."
        - yarn type-check
        
        # Build the application with optimizations
        - echo "🏗️ Building application in $BUILD_MODE mode..."
        - |
          if [ "$BUILD_MODE" = "production" ]; then
            yarn build
          else
            yarn build
          fi
        
        # Verify build output
        - echo "✅ Build completed successfully"
        - ls -la dist/
        
        # Build validation
        - |
          if [ ! -d "dist" ]; then
            echo "❌ Build directory not created"
            exit 1
          fi
        
        - |
          if [ ! -f "dist/index.html" ]; then
            echo "❌ index.html not found in build output"
            exit 1
          fi
        
        # Check build size
        - BUILD_SIZE=$(du -sh dist/ | cut -f1)
        - echo "📦 Build size: $BUILD_SIZE"
        
        # Warn if build is too large
        - |
          BUILD_SIZE_MB=$(du -sm dist/ | cut -f1)
          if [ "$BUILD_SIZE_MB" -gt 50 ]; then
            echo "⚠️ Warning: Build size is large ($BUILD_SIZE_MB MB)"
          fi
        
    postBuild:
      commands:
        # Generate comprehensive build report
        - echo "📊 Build Summary:"
        - echo "Branch: $AWS_BRANCH"
        - echo "Commit: $AWS_COMMIT_ID"
        - echo "Build size: $(du -sh dist/ | cut -f1)"
        - echo "Number of files: $(find dist/ -type f | wc -l)"
        - echo "Build timestamp: $(date)"
        
        # Health check - ensure critical files exist
        - test -f dist/index.html && echo "✅ index.html found" || (echo "❌ index.html missing" && exit 1)
        - test -f dist/robots.txt && echo "✅ robots.txt found" || echo "⚠️ robots.txt missing"
        - test -f dist/sitemap.xml && echo "✅ sitemap.xml found" || echo "⚠️ sitemap.xml missing"
        
        # Check for common build issues
        - |
          if find dist/ -name "*.map" | grep -q .; then
            echo "⚠️ Source maps found in production build"
          fi
        
        # Optional: Run basic accessibility check if pa11y is available
        - |
          if command -v pa11y >/dev/null 2>&1; then
            echo "🔍 Running accessibility check..."
            pa11y http://localhost:8080 --reporter cli || echo "⚠️ Accessibility check failed (non-blocking)"
          else
            echo "ℹ️ Accessibility check skipped (pa11y not installed)"
          fi || true
        
        # Final success message
        - echo "🎉 Amplify build completed successfully!"

  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
    
  cache:
    paths:
      - node_modules/**/*
      - ~/.yarn-cache/**/*
      - ~/.npm/**/*
      
  environment:
    variables:
      # Optimize build performance
      NODE_OPTIONS: '--max-old-space-size=4096'
      
      # Skip source maps for faster builds in production
      GENERATE_SOURCEMAP: 'false'
      
      # Optimize webpack builds
      NODE_ENV: 'production'
      
      # Enable build optimizations
      VUE_CLI_SKIP_ESLINT: 'false'  # Keep ESLint for quality
      
      # Memory optimization for large builds
      UV_THREADPOOL_SIZE: '16'
      
      # Yarn optimizations
      YARN_CACHE_FOLDER: '~/.yarn-cache'
      YARN_ENABLE_IMMUTABLE_INSTALLS: 'false'
      
      # Build optimizations
      VUE_CLI_MODERN_MODE: 'true'
      VUE_CLI_CSS_EXTRACT: 'true'